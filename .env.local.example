# MongoDB Atlas connection string (free M0 cluster)
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/ravnoortracker?retryWrites=true&w=majority"

# Random secret for NextAuth - generate with: openssl rand -base64 32
AUTH_SECRET="paste-a-random-generated-secret-here"
